# Production build: served by nginx on the app EC2, sharing origin with
# api-gateway (/api) and ws-gateway (/socket.io) via reverse proxy.

VITE_API_URL=/api

# VITE_WS_URL intentionally unset — socket.io defaults to
# window.location.origin, so the bundle works behind any ALB DNS without
# rebuilding.
