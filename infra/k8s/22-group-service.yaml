apiVersion: batch/v1
kind: Job
metadata:
  name: group-service-migrate
  namespace: messageme
spec:
  backoffLimit: 5
  template:
    spec:
      restartPolicy: OnFailure
      containers:
        - name: migrate
          image: 978714898304.dkr.ecr.us-east-1.amazonaws.com/messageme/group-service:dev
          imagePullPolicy: IfNotPresent
          workingDir: /app/services/group-service
          command: ["npx", "prisma", "db", "push", "--skip-generate", "--accept-data-loss"]
          env:
            - name: DATABASE_URL
              value: "postgresql://messageme:messageme@postgres:5432/messageme_group?schema=public"
---
apiVersion: v1
kind: Service
metadata:
  name: group-service
  namespace: messageme
spec:
  selector:
    app: group-service
  ports:
    - name: grpc
      port: 50053
      targetPort: 50053
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: group-service
  namespace: messageme
spec:
  replicas: 1
  selector:
    matchLabels:
      app: group-service
  template:
    metadata:
      labels:
        app: group-service
    spec:
      containers:
        - name: group-service
          image: 978714898304.dkr.ecr.us-east-1.amazonaws.com/messageme/group-service:dev
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 50053
          env:
            - name: NODE_ENV
              valueFrom: { configMapKeyRef: { name: messageme-config, key: NODE_ENV } }
            - name: GRPC_HOST
              valueFrom: { configMapKeyRef: { name: messageme-config, key: GRPC_HOST } }
            - name: GRPC_PORT
              value: "50053"
            - name: DATABASE_URL
              value: "postgresql://messageme:messageme@postgres:5432/messageme_group?schema=public"
